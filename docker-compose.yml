services:
  frontend:
    image: hujiashuai6/234-deploylab-frontend:latest
    pull_policy: always
    ports:
      - "8081:80"
    environment:
      - VITE_GRAPHQL_URI=http://98.84.50.30:8082/graphql
      - VITE_SERVER_URI=http://98.84.50.30:8082
    restart: unless-stopped
